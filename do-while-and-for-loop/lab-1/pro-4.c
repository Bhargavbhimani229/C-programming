#include <stdio.h>
int main()
{
   int row,i,j,sum;
   printf("Enter a row:");
   scanf("%d",&row);
   for(i=1 ; i<=row ; i++)
   {
      for(j=1 ; j<=i ; j++)
      {
        
      }
   }
    return 0;
}
